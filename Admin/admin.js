// document.addEventListener("DOMContentLoaded", function () {
//   // Set name and role dynamically from localStorage (if available)
//   const name = localStorage.getItem("name") || "Admin";
//   const role = localStorage.getItem("role") || "Administrator";

// //   document.getElementById("welcomeText").textContent = `Hi, ${name}`;
// //   document.getElementById("dropdownName").textContent = name;
// //   document.getElementById("dropdownRole").textContent = role;
// });

// // Logout confirmation
// function confirmLogout() {
//   if (confirm("Are you sure you want to logout?")) {
//     localStorage.clear();
//     sessionStorage.clear();
//     window.location.href = "/login.html"; // change this to your login page path
//   }
// }